describe("Confirm that sounds work in an actual browser.", () => {
    // TODO: Find a viable approach.
    // it("Should not have played any sound on startup.", () => {
    //     browser.debug();
    //     browser.browserA.startAnalyzing();
    //     browser.browserA.pause(1000);
    //     const stats = browser.browserA.getStats(1000);

    //     expect(stats.max.audio.outbound.inputLevel).toBe(0);
    // });

    // Should play sound when an action is added

    // Should play sound when the program is run.

    // Should not play sound when the sound is muted and an action is added.

    // Should not play sound when the sound is muted and the program is run.

});